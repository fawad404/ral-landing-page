import CaregiverApplyPage from '@/components/CaregiverApplyPage';

export const metadata = {
  title: 'Join as a Caregiver — RAL Connect',
  description: 'Apply to the RAL Connect Caregiver Directory and connect with assisted living facilities across Arizona.',
};

export default function Page() {
  return <CaregiverApplyPage />;
}
