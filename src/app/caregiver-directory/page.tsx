import CaregiverDirectoryPage from '@/components/CaregiverDirectoryPage';

export const metadata = {
  title: 'Caregiver Directory — RAL Connect',
  description: 'Browse qualified caregivers available across Arizona. Find available shifts, credentials, and contact caregivers directly.',
};

export default function Page() {
  return <CaregiverDirectoryPage />;
}
